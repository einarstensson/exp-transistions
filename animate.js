$(document).ready(function() {

    /* This adds the overlay-inc class to the element, which triggers the
    animations. */
    $("#change").on('click', function(){
        $(".overlay").addClass("overlay-inc");
    });

});
